
import NodosAcciones from './NodosAcciones';

const Sidebar = ({ collapsed, toggleSidebar }) => {
  return <NodosAcciones collapsed={collapsed} toggleSidebar={toggleSidebar} />;
};

export default Sidebar;
