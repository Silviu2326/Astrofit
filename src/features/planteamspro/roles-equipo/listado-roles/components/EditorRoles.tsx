
import React from 'react';

const EditorRoles: React.FC = () => {
  return (
    <div>
      <h2>Editor Visual de Roles (Drag & Drop)</h2>
      {/* Lógica para el editor visual de roles */}
    </div>
  );
};

export default EditorRoles;
