// Este componente ha sido reemplazado por la funcionalidad integrada en VideoGrid
// Se mantiene por compatibilidad pero ya no se usa en la nueva interfaz
import React from 'react';

const VideotecaPrivada: React.FC = () => {
  return null;
};

export default VideotecaPrivada;
