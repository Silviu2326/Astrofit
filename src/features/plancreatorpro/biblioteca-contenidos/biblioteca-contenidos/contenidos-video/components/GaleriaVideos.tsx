// Este componente ha sido reemplazado por VideoGrid
// Se mantiene por compatibilidad pero ya no se usa en la nueva interfaz
import React from 'react';

const GaleriaVideos: React.FC = () => {
  return null;
};

export default GaleriaVideos;
