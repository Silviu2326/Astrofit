// Este componente ha sido reemplazado por CategoryTabs
// Se mantiene por compatibilidad pero ya no se usa en la nueva interfaz
import React from 'react';

interface EtiquetasVideoProps {
  onSelectTag?: (tag: string | null) => void;
}

const EtiquetasVideo: React.FC<EtiquetasVideoProps> = ({ onSelectTag }) => {
  return null;
};

export default EtiquetasVideo;
